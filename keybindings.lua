return {
  left_directional = 'a',
  right_directional = 'd',
  up_directional = 'w',
  down_directional = 's',
  
  diagonal_aim = 'lshift',
  
  shoot = 'l',
  jump =  ';',
  
  drop =  's',
  
  interact = 'enter',
  
  debug_mode = 'f4',
  
  force_respawn = 'k',        -- only works in debug mode
  force_reset = 'backspace'   -- only works in debug mode
}